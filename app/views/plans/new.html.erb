<div class="row justify-content-center login">
  <div class="col-sm-12">
    <div class="card">
      <div class="card-header">
        <strong>New Plan</strong>
        <%= link_to 'Back', plans_path, class:  'btn btn-sm btn-primary pull-right'%>
      </div>
      <%= render 'form', plan: @plan %>
    </div>
  </div>
</div>