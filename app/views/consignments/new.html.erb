<div class="row justify-content-center login">
  <div class="col-sm-12">
    <div class="card">
      <div class="card-header">
        <strong>New Delivery</strong>
        <%= link_to 'Back', consignments_path, class:  'btn btn-sm btn-primary pull-right'%>
      </div>
      <%= render 'form', consignment: @consignment %>
    </div>
  </div>
</div>
